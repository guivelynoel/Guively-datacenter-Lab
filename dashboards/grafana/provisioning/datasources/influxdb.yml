apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    isDefault: true
    jsonData:
      version: Flux
      organization: guively-lab
      defaultBucket: datacenter
    secureJsonData:
      token: ${INFLUXDB_TOKEN}
